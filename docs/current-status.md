# Текущее состояние и передача другой LLM

Обновлено 15.09.2026. Это точка продолжения работы в данном checkout. Последние функциональные проверки выполнены 14.09; 15.09 обновлена документация и проверено локальное состояние Git. Production сегодня повторно не опрашивался.

## Начать здесь

1. Прочитать этот файл, затем [AGENTS.md](../AGENTS.md).
2. Проверить `git status --short`: реализация голосования ещё не закоммичена. Не сбрасывать рабочее дерево и не заменять его свежим clone.
3. Для деталей открыть [отчёт реализации](../active/demo-voting-2026-09-15/implementation.md) и [независимую проверку](../active/demo-voting-2026-09-15/security-review.md).
4. Ближайшая задача: публикация подготовленного голосования, если Дмитрий её подтвердит. Последний запрос пользователя был только обновить файлы/статусы для передачи другой LLM; это не ответ на предложение commit/push/deploy.

`active/` игнорируется Git. При продолжении в этой папке артефакты доступны; при переносе в другую среду нужно отдельно перенести незакоммиченные исходники и `active/demo-voting-2026-09-15/`. Одного клона GitHub сейчас недостаточно.

## Что уже опубликовано, а что только подготовлено

| Работа | Статус | Основание |
|---|---|---|
| Многопоточность и запуск flow-2 | Завершены ранее; августовские планы исторические | [QA](../supabase/stage_5_qa_2026-08-03.md), [запуск](cohort-2-launch-status.md) |
| Согласование материалов/ДЗ недели 5 с основным курсом | Опубликовано ранее, commit `2edffde` | [Отчёт публикации](../active/week5-lms-2026-09-05/deployment.md) |
| Записи 03.09, 08.09, 11.09 | Миграция 021 применена в Supabase 14.09; frontend-деплой не требовался | [Отчёт](../active/recordings-2026-09-14/publication.md) |
| Новый интерфейс демо-дня и расчёт равных голосов | Готовы локально, проверки пройдены; не опубликованы | Исходники и QA ниже |
| Миграции голосования 022 и 023 | Проверены на локальном PostgreSQL, в production не применены | Последняя read-only проверка 14.09: новых таблиц нет, Paul ещё не в flow-2 |
| Commit/push/deploy нового голосования | Не выполнялись; подтверждение запрошено, ответа ещё нет | Последняя договорённость с пользователем |

Ветка `master`, HEAD на момент передачи `2edffde`. Production LMS: https://ai-bootcamp-lms.vercel.app. Основной проект курса: `/Users/dima/Documents/Cursor/AI Agent Bootcamp`, материалы в `VC_Bootcamp/`. Этот проект содержания в последнем этапе не изменялся.

## Принятый формат демо-дня 15 сентября

- Ровно пять проектов: Мадина, Алёна, Руслан, Ксюша, Елена.
- Голосуют участники и ведущие. Каждый голос имеет одинаковый вес.
- Три критерия по 0–3: польза, работоспособность/проверка, владение решением. Среднее суммы критериев даёт результат до 9. Показывается число голосов.
- Свой проект оценивать нельзя, включая проект администратора. «Не видел / пропустить» исключается из среднего, настоящий 0 учитывается.
- Оценку можно менять до закрытия. Ведущие открывают/закрывают приём и видят агрегаты; студенты видят собственные оценки.
- ДЗ и посещаемость автоматически в рейтинг не прибавляются. При одинаковом среднем результат одинаковый; денежные призы и автоматическое распределение мест не настроены.
- Старые предложения с пятью критериями, формулой 70/30 или голосованием только двух ведущих не приняты. В `active/demo-day-2026-09-15/` они помечены архивными.

Подтверждённые аккаунты: Ксюша = «Ксения» (ассистент, администратор и участник); Елена = «Зарема»; Паша-тьютор = «Paul». Helen из предыдущего потока; другой «Павел» не используется. 023 добавляет Paul только membership `flow-2/expert`, без изменения глобальной роли. Лишний тестовый профиль исключён из конкурсного списка; аккаунты и остальные membership не удаляются.

## Домашние задания и посещаемость

Срез LMS 14.09, ДЗ1–5 со статусами submitted/reviewed. По обсуждавшейся ставке 0,2 за работу: Мадина 3 ДЗ → +0,6; Алёна 2 → +0,4; Руслан 2 → +0,4; Ксюша 0 → +0; Елена 2 → +0,4. Это предварительный расчёт факта отправки, не повторная оценка содержания. Работы вне LMS не учтены, задержка проверки не обнуляет submitted.

Воспроизводимый read-only запрос без email/UUID и текста работ:

```sql
select split_part(u.name, ' ', 1) as name,
  count(s.id) filter (where s.assignment_id between 1 and 5
    and s.status in ('submitted','reviewed')) as homework_count
from public.users u
join public.cohort_members cm on cm.user_id=u.id and cm.cohort_id='flow-2'
left join public.assignment_submissions s on s.user_id=u.id and s.cohort_id='flow-2'
where split_part(u.name, ' ', 1) in ('Мадина','Алена','Руслан','Ксения','Зарема')
group by u.id,u.name;
```

Посещаемость достоверно не восстановлена: `platform_visits` отражает входы в LMS, не присутствие на встречах. Отсутствие реплики в транскрипте не означает отсутствие участника. Не начислять выборочные нули.

## Файлы текущего изменения

| Файл | Назначение |
|---|---|
| `src/components/projects/DemoDayVoting.tsx` | Карточки, подсказки, сохранение, пропуск, панель ведущих; sessionStorage-черновики по viewer/cohort/candidate; защита от старых ответов и повторного открытия при закрытии |
| `src/lib/projects/demoVoting.ts` | Шкалы и валидация трёх оценок |
| `src/app/(dashboard)/projects/page.tsx` | Новый компонент только для flow-2 |
| `src/app/(dashboard)/admin/projects/page.tsx` | Новые агрегаты для flow-2 |
| `src/app/(dashboard)/admin/final-ratings/page.tsx` | Тот же новый расчёт для flow-2 |
| `supabase/migrations/022_demo_day_equal_voting.sql` | Отдельные настройки/кандидаты/голоса, RLS и RPC; начальное состояние закрыто |
| `supabase/migrations/023_demo_day_roster_bindings.sql` | Уникальные привязки пяти аккаунтов и cohort expert для Paul, транзакционный отказ при неоднозначности |
| `tests/demo-voting.test.mjs` | Настоящий ноль, пропуск, неполные/некорректные оценки, dirty state |
| `.gitignore` | Добавлен `.env*` |

Отдельно уже лежали untracked 020 и 021: это миграции записей, не новый backend голосования. 021 подтверждена отчётом применения. 020 относится к записи урока 8 от 01.09; её статус перед любым применением сверить с migration history. Не запускать все untracked SQL подряд и не включать их автоматически в коммит голосования.

## Проверенный checkpoint

- `npm test`: 18/18 passed.
- `npm run lint`: без ошибок/предупреждений.
- `npm run build`: успешно, включая TypeScript и 21 страницу.
- Независимый локальный PostgreSQL: 39 assertions/denials; атомарный rollback неоднозначного binding; две настоящие гонки save/close. Синтетические аккаунты, production не использовался.
- Браузер: 12 сценариев, ошибок JavaScript нет. Включены сохранение черновика при ошибке/навигации, ноль/skip, поздний reload после save, два ведущих при закрытии, self-vote и доступ студента.
- Визуально просмотрены desktop 1440 и mobile 390, включая таблицу ведущих. На телефоне таблица прокручивается горизонтально.
- Независимый security-review после исправлений: блокеров в scope нового изменения нет. Это не полный аудит всей LMS и зависимостей.

Доказательства: [browser-qa.json](../active/demo-voting-2026-09-15/browser-qa.json), [desktop](../active/demo-voting-2026-09-15/desktop.png), [mobile](../active/demo-voting-2026-09-15/mobile-top.png), [security-review](../active/demo-voting-2026-09-15/security-review.md). SQL harness и логи сохранены в `active/demo-voting-2026-09-15/sql-qa/`; не использовать тестовый bootstrap на production. Браузерный harness `browser-qa.cjs` работает с локальными mock-сервисами на 3067/8767; не запускать одновременно с build в том же `.next`.

## Следующий шаг после подтверждения публикации

1. Сверить рабочее дерево, migration history и текущую схему production. Не предполагать, что проверка 14.09 всё ещё свежая. Supabase MCP project ref: `psjfokjdghhcxfqzrxpw`.
2. Проверить доступность пяти владельцев и уникальный Paul; сохранить обезличенный baseline первого потока. Не выводить секреты, email, тексты работ или полные auth rows.
3. Применить 022, затем 023 через управляемые транзакционные миграции. Если 023 отклонит привязку, устранить конкретное расхождение; не обходить защиту и не открывать голосование с неполным составом.
4. Проверить: пять привязок, `is_open=false`, ноль новых голосов, Paul как cohort expert, первый поток неизменён. Legacy project_votes/gamification не переносить в новую таблицу.
5. Commit/push только согласованного изменения, дождаться Vercel deploy и проверить текущую production `/projects` для flow-2. Паша с глобальной ролью student управляет через `/projects`: middleware `/admin/*` требует глобальную роль, её повышать ради голосования не нужно.
6. Открыть голосование перед оцениванием через панель ведущих. Проверить полноту числа голосов перед объявлением мест. Не оставлять пробные голоса от имени настоящих участников.
7. Внести точные migration versions, commit, deployment и результаты live QA в этот статус и отчёт публикации.

## Контракты, которые нельзя потерять

- `assignment_submissions.assignment_id` — номер ДЗ, не FK к `assignments.id`. В LMS flow-2 семь ДЗ; CSV курса исторически содержит шесть, смещение описано в [CLAUDE.md](../CLAUDE.md).
- Flow-2 тексты ДЗ и материалы находятся в `src/lib/program/flow2Assignments.ts` и `flow2Materials.ts`; flow-1 сохраняет свою редакцию. Отчёты недели 5 не подменяют фактуру текущих сдач.
- Даты, записи и доступы потоков не наследовать через fallback. Protected `/artifacts/flow-2` требуют доступ к потоку и `no-store`.
- `useCohort().isPrivileged` основан на видимости потока и не является надёжным признаком роли. Новое голосование использует серверный `can_manage`; общий рефакторинг ролей здесь не выполнялся.
- Прямой `increment_points` закрыт для клиента. Глобальные схемы `functions.sql`/`triggers.sql` не заменяют актуальную цепочку миграций.

## Связанные документы

- [Навигация](README.md), [README проекта](../README.md).
- [Инструкции Codex](../AGENTS.md), [контекст Claude](../CLAUDE.md).
- [Исторический план](../plans.md), [исторический запуск flow-2](cohort-2-launch-status.md).
- [Локальный отчёт реализации](../active/demo-voting-2026-09-15/implementation.md).
